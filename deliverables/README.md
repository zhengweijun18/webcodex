# WebCodex Zero-Quota handoff package

Current package:

`WebCodex-Zero-Quota-3082590b-macOS-Intel.zip`

Validated source baseline:

- Branch: `vue-lsp-native-main`
- Commit: `3082590b87de6f023d775c98f8a082df6b4168c4`
- Desktop: `0.4.1`
- Platform: macOS Intel / `darwin-x64`
- Context Bridge: `0.5.0`
- Native Codex model turns observed by the packaged verification: `0`

The ZIP contains the installable Desktop, exact source snapshot, Zero-Quota Context Bridge, machine-readable verification reports, checksums, and the plain-language capability overview.

## SHA256

See [`SHA256SUMS.txt`](SHA256SUMS.txt).

The full unpacked handoff directory is intentionally not committed separately here because it would duplicate the ZIP contents in Git history. The local maintenance checkout keeps both the unpacked directory and the ZIP for future package updates.
